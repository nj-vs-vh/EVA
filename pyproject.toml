[tool.black]
line-length = 100
extend-exclude = '''
(
    ^/(model|data)/.*
)
'''


[tool.isort]
profile = 'black'
extend_skip = ["model", "data", ".devenv"]


[tool.mypy]
exclude = [
    "^model",
    "^data",
]
